#include <iostream>
#include <math.h>
using namespace std;

int main(){
	int a=10, b=9, c=11;
	float Ans;
	Ans = sqrt(((pow(b,3)-(a*a)*pow(b,19))/(pow(c,3)+pow(a,3)*sqrt(9))));
	return 0;
}
